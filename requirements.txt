# Hardcode versions so older Ubuntu's and other distros that are
# removing PIP2 don't go crazy
#
# Won't be needed once moved to Python3 and we have some control over
# installations--yep, need proper package management and stuff, but
# this is what we tested with.
asciimatics==1.11.0
Jinja2==2.11.3
PyYAML==5.3.1
opencv-python==4.1.2.30
functools32==3.2.3.post2
imutils==0.5.3
junit_xml==1.9
numpy==1.7.1
# ascimatics will pull this and 
Pillow==6.2.2
ply==3.11
pykwalify==1.7.0
pymongo==3.11.0
python-Levenshtein==0.12.0
requests
setuptools
tabulate==0.8.7
