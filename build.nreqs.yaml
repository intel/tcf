#
# Install with::
#
#   $ tcf.git/nreqs.py install base.nreqs.yaml
#
git:

make:

sphinx:
  dnf: python3-sphinx

sphinx_rtd_theme:
  dnf: python3-sphinx_rtd_theme

pyusb:
  dnf: python3-pyusb

setuptools:
  # >= 0.7 due to https://osv.dev/vulnerability/GHSA-27x4-j476-jp5f
  distro: python3-setuptools
